    # for i in range(1,int(math.sqrt(max(w-1,l-1)))+1):
    #     if (w-1)%i==0 and (l-1)%i==0:
    #         res.append(i)
    #         if max(w-1,l-1)//i <= w-1 and max(w-1,l-1)//i <= l-1:
    #             res.append(max(w-1,l-1)//i)