#include <bits/stdc++.h>
using namespace std;

int main()
{
    int test = 0;
    cin >> test;
    while (test--)
    {
        long long n;
        cin >> n;
        cout << (long long)sqrt(n - 1) << endl;
    }
}